# 2026-07-06 工具栏悬浮菜单与显示优化

## 修改内容

- 将选中文字后的浮动工具栏统一为白色背景和一致的图标颜色，减少按钮之间的视觉割裂。
- 优化字体颜色按钮的布局，让 `A` 和下拉箭头靠近显示。
- 将原本的文本下拉符号 `⌄` 改为 CSS 绘制的小箭头，避免在“正文”右侧显示成小黑点。
- 字体颜色和正文格式菜单现在会在鼠标离开按钮/菜单区域时自动关闭，保留点击空白处关闭的逻辑。

## 验证

- `npm test`
- `npm run typecheck`
- `npm run lint`
- `npm run build`
